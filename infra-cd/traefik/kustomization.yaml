apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: infra

resources:
  - deployment.yaml
  - traefik_crd.yaml
  - traefik_rbac.yaml

configMapGenerator:
  - name: traefik-https-cfg
    files:
      - traefik.toml

